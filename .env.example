# Copy this file to .env and fill in your own value. Never commit .env.
#
# API key for jev (TypeSafe AI, System One model) — used by the LogClassifier
# adapter (from increment 3 onward) to call https://api.typesafe.ai/v1/systemone.
JEV_API_KEY=
